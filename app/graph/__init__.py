"""LangGraph pipeline package.

Phase 1: minimal `state` (ClaimState) + `builder` (a one-node scaffold that
proves the graph compiles and runs). Nodes for classification, verification,
extraction, adjudication, and explanation arrive in later phases.
"""
